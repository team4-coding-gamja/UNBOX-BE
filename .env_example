# 기본 환경변수 예시
# 실제 사용 시 .env.test 또는 .env.mvp를 .env로 복사하여 사용

# PostgreSQL 설정 (기본값)
DB_DRIVER_CLASS_NAME=org.postgresql.Driver
DB_URL=jdbc:postgresql://localhost:5432/unboxdb
DB_NAME=unboxdb
DB_USERNAME=admin
DB_PASSWORD=password123!

# Redis 설정
SPRING_DATA_REDIS_HOST=localhost
SPRING_DATA_REDIS_PORT=6379

# JWT 설정
SPRING_JWT_SECRET=your-jwt-secret-key-here