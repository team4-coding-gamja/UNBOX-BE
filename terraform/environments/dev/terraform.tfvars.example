# =============================================================================
# UNBOX 백엔드 인프라 설정 예시 파일
# 이 파일을 복사하여 terraform.tfvars로 이름을 변경하고 실제 값으로 수정하세요
# =============================================================================

# 프로젝트 이름 (기본값 사용 권장)
project_name = "unbox-mvp"

# SSH 공개키 (필수 설정)
# 1. 터미널에서 ssh-keygen -t rsa -b 4096 -f ~/.ssh/unbox_key 실행
# 2. ~/.ssh/unbox_key.pub 파일 내용을 아래에 붙여넣기
# 3. 따옴표 안에 전체 내용을 한 줄로 입력
public_key = "ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAACAQC... (여기에 실제 공개키 내용 입력)"

# RDS 사용 여부
# true:  AWS RDS PostgreSQL 사용 (유료, 데이터 영구 보존)
# false: H2 인메모리 DB 사용 (무료, 서버 재시작시 데이터 초기화)
use_rds = true

# =============================================================================
# 설정 완료 후 인프라 배포 명령어
# =============================================================================
# 1. cd terraform/environments/dev
# 2. terraform init
# 3. terraform plan -var="use_rds=true"
# 4. terraform apply -var="use_rds=true"
#
# 또는 전체 배포 스크립트 사용:
# ./scripts/setup-mvp.sh
#
# 배포 완료 후 출력되는 EC2 IP로 Swagger UI 접속:
# http://{EC2_PUBLIC_IP}:8080/swagger-ui/index.html