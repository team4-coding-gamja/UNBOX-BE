spring:
  # 로컬 개발 환경용 설정 (Docker Compose 인프라와 연결)
  config:
    activate:
      on-profile: local

  datasource:
    driver-class-name: org.postgresql.Driver
    # Docker Compose의 Postgres 포트(5432)에 접속. DB명은 unbox_user
    url: jdbc:postgresql://localhost:5432/unbox_user
    username: postgres
    password: joshua0526

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

  kafka:
    bootstrap-servers: localhost:9092

  jwt:
    secret: v3RyS3cr3tK3yF0rJwTS1gn1ngD0N0tSh4r3Th1sK3yW1thAny0n3

logging:
  level:
    root: INFO
    com.example.unbox_user: DEBUG

# Service URLs (Localhost ports)
user-service:
  url: http://localhost:8081
product-service:
  url: http://localhost:8082
trade-service:
  url: http://localhost:8083
order-service:
  url: http://localhost:8084
payment-service:
  url: http://localhost:8085
